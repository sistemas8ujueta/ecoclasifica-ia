# URL del backend (server/index.js) que usan las apps Android/iOS.
# Copia este archivo como ".env" y pon la IP de tu PC (misma red Wi-Fi)
# o la URL pública del servidor desplegado. Luego: npm run build && npx cap sync
VITE_API_URL=http://192.168.1.68:4000
